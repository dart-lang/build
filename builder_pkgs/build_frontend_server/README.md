# build_frontend_server

A package to manage the Frontend Server worker for `build_runner` and `webdev`.

## Usage

This package is typically not used directly by end users. It is used by `build_web_compilers` and `webdev` to interact with the Frontend Server for expression evaluation, hot reload, and modular compilation.
