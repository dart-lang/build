name: source_gen_example

environment:
  sdk: ">=2.3.0 <3.0.0"

dependencies:
  analyzer: ">=0.37.0 <0.40.0"
  build: ^1.0.0
  source_gen: ^0.9.0

dependency_overrides:
  source_gen:
    path: ../source_gen
