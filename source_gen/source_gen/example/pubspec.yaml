name: source_gen_example

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  analyzer: '>=2.1.0 <4.0.0'
  build: ^2.0.0
  source_gen: any

dependency_overrides:
  source_gen:
    path: ../source_gen
