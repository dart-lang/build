name: source_gen_example

environment:
  sdk: ">=2.10.0 <3.0.0"

dependencies:
  analyzer: ">=0.37.0 <0.41.0"
  build: ^1.4.0
  source_gen: ^0.9.0

dependency_overrides:
  source_gen:
    path: ../source_gen
