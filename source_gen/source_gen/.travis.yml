language: dart

dart:
  - dev
  # Flutter Alpha @ v0.0.23
  - 2.0.0-dev.19.0
dart_task:
  - test
  - dartfmt
  - dartanalyzer

# Only building master means that we don't run two builds for each pull request.
branches:
  only: [master]

cache:
  directories:
    - $HOME/.pub-cache
