language: dart
dart:
  - dev
  - stable

dart_task:
  - test
  - dartfmt: sdk
  # TODO: restore https://github.com/dart-lang/build/issues/842
  - dartanalyzer: --fatal-warnings .

matrix:
  exclude:
    - dart: stable
      dart_task:
        dartfmt: sdk
