dart:
  - dev/release/2.6.0-dev.1.0

stages:
  - analyze_and_format:
    - group:
      - dartfmt: sdk
      - dartanalyzer: --fatal-infos --fatal-warnings .
  #- unit_test:
  #  - test: --run-skipped
