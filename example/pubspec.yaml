name: example

environment:
  sdk: '>=2.0.0-dev <3.0.0'

dependencies:
  build: ^0.12.1
  # Not imported in code, but used to constrain `build.yaml` requirements
  build_config: ^0.2.6

dev_dependencies:
  build_runner: ^0.8.1
  build_web_compilers: ^0.4.0
