name: built_value
version: 8.13.1-wip
description: >
  Value types with builders, Dart classes as enums, and serialization.
  This library is the runtime dependency.
repository: https://github.com/dart-lang/build/tree/master/built_types/built_value
topics:
 - built-value

resolution: workspace

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  built_collection: ^5.0.0
  collection: ^1.15.0
  fixnum: ^1.0.0
  meta: ^1.3.0

dev_dependencies:
  dart_flutter_team_lints: ^3.1.0
  test: ^1.16.0
