name: bazel_codegen
version: 0.0.1
description: Create code generating scripts compatible with bazel.
author: Dart Team <misc@dartlang.org>
homepage: https://github.com/dart-lang/build

environment:
  sdk: '>=1.9.1 <2.0.0'

dependencies:
  analyzer: ">=0.27.1 <0.30.0"
  args: ^0.13.7
  bazel_worker: ^0.1.1
  build: ^0.6.3
  build_barback: ^0.0.1
  logging: ^0.11.2
  path: ^1.1.0
  protobuf: ^0.5.3

dev_dependencies:
  test: ^0.12.0
