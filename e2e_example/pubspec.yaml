name: e2e_example

environment:
  sdk: '>=1.9.1 <2.0.0'

dependencies:
  build: ^0.9.0
  build_barback: ^0.2.0

dev_dependencies:
  build_runner: ^0.3.2

transformers:
- e2e_example:
    $include: 'web/*.txt'

dependency_overrides:
  build:
    path: ../build
  build_runner:
    path: ../build_runner
  build_barback:
    path: ../build_barback
