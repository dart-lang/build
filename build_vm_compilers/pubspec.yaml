name: build_vm_compilers
version: 0.1.0-dev

dependencies:
  build: ^0.12.0
  build_modules: ^0.3.0
  path: ^1.6.0

dev_dependencies:
  build_runner: any
  test: ^1.0.0
  test_descriptor: ^1.1.0
  _test_common:
    path: ../_test_common

dependency_overrides:
  build_modules:
    path: ../build_modules
