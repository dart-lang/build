dart:
  - stable/raw/2.0.0

stages:
  - analyze_and_format:
    - group:
        - dartfmt: sdk
        - dartanalyzer: --fatal-infos --fatal-warnings .
  - unit_test:
    - command: pub run build_runner test --delete-conflicting-outputs

cache:
  directories:
    - .dart_tool/build
